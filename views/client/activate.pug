extends ../layouts/main-layout.pug

block content
    main.reset-password
        .container
            form.reset-password__form#activate-form
                input#captcha(type="hidden", name="captcha", value="")
                .form-group
                    .mb-3
                        label.form-label(for="email-input") Your Username
                        input#username-input.form-control(type="text", name="username")
                    .mb-3
                        .g-recaptcha(data-sitekey=captchaPublicKey, data-callback="onHuman", style="margin-bottom: 1rem;")
                input.btn.btn-success(type="submit", value="Återaktivera konto")